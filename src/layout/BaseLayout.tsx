import React from 'react'

interface BaseLayout {
    showHeader?: boolean;
    showFooter?: boolean;
}

export default function BaseLayout({ }: Props) {
    return (
        <div>BaseLaout</div>
    )
}